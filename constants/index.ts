import images from "./images";

const APP = {
	NAME: "Daily X*",
	DESC: "Abhi ni pta kuch",
};

const AUTH_CONFIG = {
	androidClientId: "",
	iosClientId: "",
	webClientId: "",
};

const STORAGE_KEYS = {
	USER: "@user",
	TOKEN: "@token",
};

export { images, APP, AUTH_CONFIG, STORAGE_KEYS };
